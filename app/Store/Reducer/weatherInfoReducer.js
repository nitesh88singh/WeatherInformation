

const initialState = {
    weatherInfoList: []
}

function weatherInfoReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}
export default weatherInfoReducer;